<Window x:Class="PL.Vol.VolunteerWindow"
       
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        xmlns:pl="clr-namespace:PL"
         mc:Ignorable="d">

    <Window.Resources>
    </Window.Resources>
    <Grid Height="540" Width="877">
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Width" Value="150"/>
            </Style>
            <Style TargetType="Control" x:Key="InputStyle">
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Width" Value="200"/>
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Grid.Resources>



        <Grid.RowDefinitions>
            <!-- 15 שורות לפי מספר השדות -->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>


        <!-- ID -->
        <Label Content="Id:" Grid.Row="0" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.Id, Mode=TwoWay}" 
         Grid.Row="0" Grid.Column="1" 
         Style="{StaticResource InputStyle}" 
         IsReadOnly="{Binding IsUpdateMode}" />


        <!-- FullName -->
        <Label Content="Full Name:" Grid.Row="1" Grid.Column="0"/>
        <TextBox  Text="{Binding CurrentVolunteer.FullName,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="1" Grid.Column="1" Style="{StaticResource InputStyle}"/>

        <!-- Phone -->
        <Label Content="Phone:" Grid.Row="2" Grid.Column="0"/>
        <TextBox  Text="{Binding CurrentVolunteer.Phone,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="2" Grid.Column="1" Style="{StaticResource InputStyle}"/>

        <!-- Email -->
        <Label Content="Email:" Grid.Row="3" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.Email,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="3" Grid.Column="1" Style="{StaticResource InputStyle}"/>

        <!-- Password -->
        <Label Content="Password:" Grid.Row="4" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.Password,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="4" Grid.Column="1" Style="{StaticResource InputStyle}" />

        <!-- FullAddress -->
        <Label Content="Full Address:" Grid.Row="5" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.FullAddress,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="5" Grid.Column="1" Style="{StaticResource InputStyle}" />

        <!-- Role -->
        <Label Content="Role:" Grid.Row="6" Grid.Column="0"/>
        <ComboBox ItemsSource="{Binding Source={StaticResource RoleCollectionKey}}"
          SelectedValue="{Binding CurrentVolunteer.Role}" 
          Grid.Row="6" Grid.Column="1" Style="{StaticResource InputStyle}"/>


        <!-- MaxDistance -->
        <Label Content="Max Distance:" Grid.Row="7" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.MaxDistance,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}"  Grid.Row="7" Grid.Column="1" Style="{StaticResource InputStyle}"/>

        <!-- TypeOfDistance -->
        <Label Content="Type Of Distance:" Grid.Row="8" Grid.Column="0"/>
        <ComboBox ItemsSource="{Binding Source={StaticResource TypeOfDistanceCollectionKey}}"
          SelectedValue="{Binding CurrentVolunteer.TypeOfDistance, Mode=TwoWay}"
          Grid.Row="8" Grid.Column="1"
          Style="{StaticResource InputStyle}"/>

        <!-- AllCallsThatTreated -->
        <Label Content="All Calls That Treated :" Grid.Row="9" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.AllCallsThatTreated,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="9" Grid.Column="1" Style="{StaticResource InputStyle}"  />


        <!-- AllCallsThatCanceled -->
        <Label Content="All Calls That Canceled:" Grid.Row="10" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.AllCallsThatCanceled,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="10" Grid.Column="1" Style="{StaticResource InputStyle}"  />

        <!-- AllCallsThatHaveExpired -->
        <Label Content="All Calls That Have Expired:" Grid.Row="11" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.AllCallsThatHaveExpired,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="11" Grid.Column="1" Style="{StaticResource InputStyle}"  />


        <!-- Active -->
        <Label Content="Is Active :" Grid.Row="12" Grid.Column="0"/>
        <CheckBox Grid.Row="12" Grid.Column="1"
          IsChecked="{Binding CurrentVolunteer.Active, ValidatesOnExceptions=True, NotifyOnValidationError=True, Mode=TwoWay}"
          Style="{StaticResource InputStyle}" />


        <!-- TypeOfCall -->
        <Label Content="Type Of Call:" Grid.Row="13" Grid.Column="0"/>
        <ComboBox ItemsSource="{Binding Source= {StaticResource TypeOfCallCollectionKey}}"
          SelectedValue="{Binding CurrentVolunteer.TypeOfCall, Mode=TwoWay}" 
          Grid.Row="13" Grid.Column="1" Style="{StaticResource InputStyle}"/>

        <!--CallInProgress-->
        <Label Content="Call In Progress:" Grid.Column="2" Grid.ColumnSpan="2" Margin="132,0,0,0" HorizontalAlignment="Left"/>

        <!-- CallId -->
        <Label Content="Call Id :" Grid.Row="1" Grid.Column="2"/>
        <TextBox Text="{Binding CurrentVolunteer.CallInTreated.CallId,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="1" Grid.Column="3" Style="{StaticResource InputStyle}"  />

        <!-- VerbalDescription -->
        <Label Content="Verbal Description :" Grid.Row="2" Grid.Column="2"/>
        <TextBox Text="{Binding CurrentVolunteer.CallInTreated.VerbalDescription,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="2" Grid.Column="3" Style="{StaticResource InputStyle}"  />

        <!-- FullAddress -->
        <Label Content="Full Address :" Grid.Row="3" Grid.Column="2"/>
        <TextBox Text="{Binding CurrentVolunteer.CallInTreated.FullAddress,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="3" Grid.Column="3" Style="{StaticResource InputStyle}"  />

        <!-- OpenTime -->
        <Label Content="Open Time :" Grid.Row="4" Grid.Column="2"/>
        <TextBox Text="{Binding CurrentVolunteer.CallInTreated.OpenTime,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="4" Grid.Column="3" Style="{StaticResource InputStyle}"  />

        <!-- MaxTimeToFinish -->
        <Label Content="Max Time To Finish :" Grid.Row="5" Grid.Column="2"/>
        <TextBox Text="{Binding CurrentVolunteer.CallInTreated.MaxTimeToFinish,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="5" Grid.Column="3" Style="{StaticResource InputStyle}"  />

        <!-- EnterTime -->
        <Label Content="Enter Time :" Grid.Row="6" Grid.Column="2"/>
        <TextBox Text="{Binding CurrentVolunteer.CallInTreated.EnterTime,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="6" Grid.Column="3" Style="{StaticResource InputStyle}"  />

        <!-- Distance -->
        <Label Content="Distance :" Grid.Row="7" Grid.Column="2"/>
        <TextBox Text="{Binding CurrentVolunteer.CallInTreated.Distance,ValidatesOnExceptions=true ,NotifyOnValidationError=true,  Mode=TwoWay}" Grid.Row="7" Grid.Column="3" Style="{StaticResource InputStyle}"  />

        <!-- Status -->
        <Label Content="Status:" Grid.Row="8" Grid.Column="2"/>
        <ComboBox ItemsSource="{Binding Source= {StaticResource StatusCollectionKey}}"
          SelectedValue="{Binding CurrentVolunteer.CallInTreated.Status, Mode=TwoWay}" 
          Grid.Row="8" Grid.Column="3" Style="{StaticResource InputStyle}"/>

        <!-- כפתור הוספה/עדכון -->
        <Button Grid.Row="13" Grid.Column="2"
        Margin="35,18,0,-23" HorizontalAlignment="Left" Width="150"
        Content="{Binding ButtonText}" Click="btnAddUpdate_Click" Grid.RowSpan="3" Grid.ColumnSpan="2"/>

    </Grid>
</Window>

