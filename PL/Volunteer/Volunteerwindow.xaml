<Window x:Class="PL.Volunteer.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="Volunteerwindow" Height="664" Width="800"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid Margin="10">
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Width" Value="150"/>
            </Style>
            <Style TargetType="Control" x:Key="InputStyle">
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Width" Value="200"/>
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <!-- 16 שורות לפי מספר השדות -->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Id -->
        <Label Content="Id:" Grid.Row="0" Grid.Column="0"/>
        <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource InputStyle}" IsReadOnly="True" Text="{Binding Id}"/>

        <!-- FullName -->
        <Label Content="Full Name:" Grid.Row="1" Grid.Column="0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource InputStyle}" Text="{Binding FullName}"/>

        <!-- Phone -->
        <Label Content="Phone:" Grid.Row="2" Grid.Column="0"/>
        <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource InputStyle}" Text="{Binding Phone}"/>

        <!-- Email -->
        <Label Content="Email:" Grid.Row="3" Grid.Column="0"/>
        <TextBox Grid.Row="3" Grid.Column="1" Style="{StaticResource InputStyle}" Text="{Binding Email}"/>

        <!-- Password -->
        <Label Content="Password:" Grid.Row="4" Grid.Column="0"/>
        <TextBox Grid.Row="4" Grid.Column="1" Style="{StaticResource InputStyle}" Text="{Binding Password}"/>

        <!-- FullAddress -->
        <Label Content="Full Address:" Grid.Row="5" Grid.Column="0"/>
        <TextBox Grid.Row="5" Grid.Column="1" Style="{StaticResource InputStyle}" Text="{Binding FullAddress}"/>

        <!-- Latitude -->
        <Label Content="Latitude:" Grid.Row="6" Grid.Column="0"/>
        <TextBox Grid.Row="6" Grid.Column="1" Style="{StaticResource InputStyle}" Text="{Binding Latitude}"/>

        <!-- Longitude -->
        <Label Content="Longitude:" Grid.Row="7" Grid.Column="0"/>
        <TextBox Grid.Row="7" Grid.Column="1" Style="{StaticResource InputStyle}" Text="{Binding Longitude}"/>

        <!-- Role -->
        <Label Content="Role:" Grid.Row="8" Grid.Column="0"/>
        <ComboBox Grid.Row="8" Grid.Column="1" Style="{StaticResource InputStyle}" SelectedItem="{Binding Role}"/>

        <!-- Active -->
        <Label Content="Active:" Grid.Row="9" Grid.Column="0"/>
        <CheckBox Grid.Row="9" Grid.Column="1" Style="{StaticResource InputStyle}" IsChecked="{Binding Active}"/>

        <!-- MaxDistance -->
        <Label Content="Max Distance:" Grid.Row="10" Grid.Column="0"/>
        <TextBox Grid.Row="10" Grid.Column="1" Style="{StaticResource InputStyle}" Text="{Binding MaxDistance}"/>

        <!-- TypeOfDistance -->
        <Label Content="Type Of Distance:" Grid.Row="11" Grid.Column="0"/>
        <ComboBox Grid.Row="11" Grid.Column="1" Style="{StaticResource InputStyle}" />

        <!-- AllCallsThatTreated -->
        <Label Content="Calls Treated:" Grid.Row="12" Grid.Column="0"/>
        <TextBox Grid.Row="12" Grid.Column="1" Style="{StaticResource InputStyle}" IsReadOnly="True" Text="{Binding AllCallsThatTreated}"/>

        <!-- AllCallsThatCanceled -->
        <Label Content="Calls Canceled:" Grid.Row="13" Grid.Column="0"/>
        <TextBox Grid.Row="13" Grid.Column="1" Style="{StaticResource InputStyle}" IsReadOnly="True" Text="{Binding AllCallsThatCanceled}"/>

        <!-- AllCallsThatHaveExpired -->
        <Label Content="Calls Expired:" Grid.Row="14" Grid.Column="0"/>
        <TextBox Grid.Row="14" Grid.Column="1" Style="{StaticResource InputStyle}" IsReadOnly="True" Text="{Binding AllCallsThatHaveExpired}"/>

        <!-- CallInTreate -->
        <!--<Label Content="Call In Treat:" Grid.Row="15" Grid.Column="0"/>
        <TextBox Grid.Row="15" Grid.Column="1" Style="{StaticResource InputStyle}" IsReadOnly="True" Text="{Binding CallInTreate}"/>-->



        <!-- כפתור הוספה/עדכון -->
        <Button Grid.Row="15" Grid.Column="1"
        Margin="139,34,0,-24" HorizontalAlignment="Left" Width="150"
        Content="{Binding ButtonText}" Click="btnAddUpdate_Click"/>

    </Grid>
</Window>
