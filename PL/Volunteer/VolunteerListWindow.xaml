<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="VolunteerListWindow" Height="640" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Label" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="0" />
            <ComboBox ItemsSource="{Binding Source={StaticResource TypeOfCallCollectionKey}}"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      Grid.Column="1"
                      SelectedValue="{Binding Path= volunteerTypeOfCall, Mode=TwoWay}"
                      SelectedValuePath="."
                      SelectionChanged="ComboBox_SelectionChanged"/>
        </Grid>
        <DataGrid Grid.Row="1"
          Margin="10"
          ItemsSource="{Binding Path=VolunteerList}"
          IsReadOnly="True"
          AutoGenerateColumns="False"
          EnableRowVirtualization="True" 
          RowDetailsVisibilityMode="VisibleWhenSelected"
          SelectedItem="{Binding SelectesVolunteer}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding FullName}" Header="FullName" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Background" Value="LightGreen"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding AllCallsThatTreated}" Header="SumTreated" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding AllCallsThatCanceled}" Header="SumCanceled" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding AllCallsThatHaveExpired}" Header="SumExpired" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding CallId}" Header="CallId" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding TypeOfCall}" Header="CallType" Width="Auto"/>
                <DataGridTemplateColumn Header="Is Active" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsEnabled="False" IsChecked="{Binding Active}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Click="Delete_click" Tag="{Binding Id}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Add"
                Grid.Row="2"
                Margin="10"
                Height="40"
                Width="100"
                HorizontalAlignment="Right"
                VerticalAlignment="Center" />
    </Grid>
</Window>
