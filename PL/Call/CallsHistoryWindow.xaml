<Window x:Class="PL.Call.CallsHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        mc:Ignorable="d"
        Title="CallsHistoryWindow" Height="450" Width="800"
    DataContext="{Binding ={RelativeSource Mode=Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Select a value for which the calls will be filtered."
           HorizontalAlignment="Left"
           VerticalAlignment="Center"
           Grid.Column="0"
           Grid.Row="0" />

            <ComboBox ItemsSource="{Binding Source={StaticResource TypeOfCallCollectionKey}}"
          Grid.Column="1"
          Grid.Row="0"
          SelectedValue="{Binding SelectedCallType, RelativeSource={RelativeSource AncestorType=Window}, Mode=TwoWay}"
          SelectedValuePath="."
          Margin="66,0,75,26"
          Grid.RowSpan="2" />

            <Label Content="Select a value for which calls will be sorted."
           HorizontalAlignment="Left"
           VerticalAlignment="Center"
           Grid.Column="0"
           Grid.Row="1" />

            <ComboBox ItemsSource="{Binding Source={StaticResource ClosedCallInListFieldCollectionKey}}"
          Grid.Column="1"
          Grid.Row="1"
          SelectedValue="{Binding SelectedSortField, RelativeSource={RelativeSource AncestorType=Window}, Mode=TwoWay}"
          SelectedValuePath="."
          Margin="66,0,75,0" />
        </Grid>

        <DataGrid Grid.Row="1"
          Margin="10"
          ItemsSource="{Binding Path=ClosedCallsList}"
          IsReadOnly="True"
          AutoGenerateColumns="False"
          EnableRowVirtualization="True" 
          RowDetailsVisibilityMode="VisibleWhenSelected"
          SelectedItem="{Binding ClosedCallsList}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding TypeOfCall}" Header="Type Of Call" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding FullAddress}" Header="Full Address" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding OpenTime}" Header="Open Time" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding TypeOfTreatment}" Header="Type Of Treatment" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>

<!--public class ClosedCallInList
    {

        public int Id { get; init; }
        public TYPEOFCALL TypeOfCall { get; init; }
        public string FullAddress { get; init; }
        public DateTime OpenTime { get; init; }
        public DateTime EntryTimeForTreatment { get; init; }
        public DateTime? EndTimeOfTreatment { get; init; }
        public FINISHTYPE? TypeOfTreatment { get; init; }
    }-->



